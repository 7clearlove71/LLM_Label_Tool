# CLAUDE.md

本文件为 Claude Code (claude.ai/code) 在此代码仓库中工作时提供指导。

## 项目状态

这是一个全新的 Python 项目。当前的 `main.py` 是 PyCharm 默认模板，需要替换为实际实现。

## 运行项目

```bash
python main.py
```

## 开发环境配置

目前尚未定义任何依赖项。随着项目发展，请添加 `requirements.txt` 或 `pyproject.toml` 来管理依赖。
